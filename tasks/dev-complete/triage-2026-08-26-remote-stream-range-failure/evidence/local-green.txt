Local verification: 2026-08-26

Rust formatting and full server suite:

  cargo fmt --manifest-path server/Cargo.toml --check
  cargo test --manifest-path server/Cargo.toml

Result: 40 passed; 0 failed.

Targeted progressive-range suite:

  cargo test progressive_ -- --nocapture

Result: 3 passed; 0 failed.

Container/runtime checks:

- `shellcheck server/container-entrypoint.sh`: PASS
- `sh -n server/container-entrypoint.sh`: PASS
- linux/amd64 Docker image build: PASS (`bkgrnd:hpx-pot-validation`)
- `/api/v1/health`: `ok`
- loopback POT provider `/ping`: version 1.3.1
- provider source listeners: rewritten to 127.0.0.1
- yt-dlp provider plugin import: PASS

The resolver false-success regression test first failed when a fake POT
candidate returned a 403 media URL, then passed after bounded candidate probing
was added and the resolver continued to web-embedded.
